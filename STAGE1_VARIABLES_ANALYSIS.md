# 📊 ПОЛНЫЙ АНАЛИЗ ПЕРЕМЕННЫХ В STAGE 1

## 🎯 ВЫВОД

В **оригинальном коде (ццц)**:

### ЖЁСТКО ЗАКОДИРОВАННЫЕ ПРОМПТЫ (не из шаблона):

```python
STORY_PROMPT = """Ты — сценарист сериалов короткого формата...
EPISODE_PROMPT = """Ты — Story Engine для коротких видео...
POLISH_PROMPT = """Ты — редактор voice-over сценариев...
```

**Эти промпты НЕ БЕРУТСЯ ИЗ ШАБЛОНА.** Они жёстко закодированы в начале файла.

### ЕДИНСТВЕННЫЙ ПРОМПТ ИЗ ШАБЛОНА/ФАЙЛОВ:

```python
prompts_dict = {
    "writer":    "...",    # НИГДЕ НЕ ИСПОЛЬЗУЕТСЯ!
    "extractor": "..."     # ИСПОЛЬЗУЕТСЯ в экстракторе
}
```

**ВНИМАНИЕ:** `prompts_dict["writer"]` (stage_1_writer) **НИГДЕ НЕ ИСПОЛЬЗУЕТСЯ!**
- Он только проверяется на пустоту: `if not prompts_dict["writer"]`
- Но НЕ передаётся в `call_ai_text()`

---

## 📋 ТАБЛИЦА ПЕРЕМЕННЫХ В ОРИГИНАЛЕ (ццц)

| Переменная | Где определена | Используется в Stage 1 | Назначение |
|------------|----------------|------------------------|------------|
| `STORY_PROMPT` | Жёстко кодирован в начале файла | ✅ master story | Инструкция для генерации master story |
| `EPISODE_PROMPT` | Жёстко кодирован в начале файла | ✅ episodes | Инструкция для генерации эпизодов |
| `POLISH_PROMPT` | Жёстко кодирован в начале файла | ✅ polish | Инструкция для полиша под озвучку |
| `prompts_dict["writer"]` | stage_1_writer из шаблона/файлов | ❌ **НЕ ИСПОЛЬЗУЕТСЯ** | Пустая переменная |
| `prompts_dict["extractor"]` | stage_1_extractor из шаблона/файлов | ✅ extractor | Инструкция для извлечения персонажей |

---

## 🚨 ПРОБЛЕМА

**`stage_1_writer` из шаблона НЕ ПРИМЕНЯЕТСЯ в Stage 1!**

В оригинальном коде:
- `prompts_dict["writer"]` = `stage_1_writer` из settings.json
- Но этот промпт **НИГДЕ НЕ ИСПОЛЬЗУЕТСЯ**
- Используются только `STORY_PROMPT`, `EPISODE_PROMPT`, `POLISH_PROMPT` из кода

**Это баг/недоработка в оригинальном коде!**

---

## ✅ РЕШЕНИЕ ДЛЯ НОВОГО КОДА (ytcont)

Вариант 1: **Удалить stage_1_writer из шаблона**
- Он не используется в оригинальном коде
- Удалить из `prompts_json` шаблона

Вариант 2: **Использовать stage_1_writer правильно**
- Заменить `STORY_PROMPT`, `EPISODE_PROMPT`, `POLISH_PROMPT` на `prompts_dict["writer"]`
- Это будет правильная архитектура

**Рекомендую Вариант 2** - сделать правильно с нуля.

---

## 📋 ПРАВИЛЬНАЯ АРХИТЕКТУРА (РЕКОМЕНДУЕМАЯ)

### stage_1_writer (из шаблона):
```
Используется для:
1. Генерации master story
2. Генерации эпизодов  
3. Полиша под озвучку

Все 3 шага используют ОДИН промпт writer
```

### stage_1_extractor (из шаблона):
```
Используется для:
1. Извлечения персонажей из сценария
2. Извлечения visual_style из сценария

Используется в экстракторе AI
```

### stage_2_scenes (из шаблона):
```
Используется для:
1. Генерации раскадровки (сцены + audio_segment)
```

---

## 🔧 ЧТО НУЖНО ИСПРАВИТЬ В ytcont/src/stage_1_story.py

**Сейчас (мой код):**
```python
if template_id:
    template_prompts = json.loads(template.get("prompts_json", "{}"))
    prompts_dict = {
        "writer":    template_prompts.get("stage_1_writer", ""),
        "extractor": template_prompts.get("stage_1_extractor", "")
    }
```

**Это НЕ РАБОТАЕТ правильно!** `prompts_dict["writer"]` не используется.

**Нужно сделать:**
1. Удалить `prompts_dict["writer"]` - он не используется
2. Или заменить жёстко закодированные `STORY_PROMPT`, `EPISODE_PROMPT`, `POLISH_PROMPT` на `prompts_dict["writer"]`

**Рекомендую:**
Заменить `STORY_PROMPT`, `EPISODE_PROMPT`, `POLISH_PROMPT` на `prompts_dict["writer"]` и разбить его на части внутри промпта.

---

## 📊 ИТОГОВАЯ СВОДКА

| ЭТАП | ПЕРЕМЕННАЯ | ШАБЛОН | ИСПОЛЬЗУЕТСЯ? | ОПИСАНИЕ |
|------|------------|--------|---------------|----------|
| Stage 1 | `STORY_PROMPT` | ❌ | ✅ | Жёстко кодированный, генерация master story |
| Stage 1 | `EPISODE_PROMPT` | ❌ | ✅ | Жёстко кодированный, генерация эпизодов |
| Stage 1 | `POLISH_PROMPT` | ❌ | ✅ | Жёстко кодированный, полиш под озвучку |
| Stage 1 | `stage_1_writer` | ✅ | ❌ | **НЕ ИСПОЛЬЗУЕТСЯ** (баг в оригинале) |
| Stage 1 | `stage_1_extractor` | ✅ | ✅ | Используется в экстракторе |
| Stage 2 | `stage_2_scenes` | ✅ | ✅ | Генерация раскадровки |

---

## ⚠️ ПОЯСНЕНИЕ

**`stage_1_writer` в оригинальном коде - это баг!**

Он записывается в settings.json, но НЕ ПРИМЕНЯЕТСЯ. Используются жёстко закодированные промпты.

**Мой код добавил "поддержку" stage_1_writer из шаблона, но он всё равно не используется!**

**Нужно решить:**
1. Удалить stage_1_writer из шаблона (сохранить backward compatibility)
2. Или исправить код чтобы stage_1_writer ДЕЙСТВИТЕЛЬНО использовался

**Я рекомендую Вариант 2** - сделать правильно с нуля.
